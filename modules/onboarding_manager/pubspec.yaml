name: onboarding_manager
description: A standardized onboarding flow manager.
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  flutter_riverpod: ^2.4.0
  ui_shell:
    path: ../ui_shell

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
